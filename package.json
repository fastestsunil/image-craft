{
  "name": "imagecraft-chrome-extension",
  "version": "1.0.0",
  "description": "Advanced image tools Chrome extension - Save and copy images in multiple formats with background removal",
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "build": "zip -r imagecraft.zip . -x '*.git*' -x 'node_modules/*' -x '*.md' -x 'package*.json' -x '.DS_Store' -x 'utils/*'",
    "test": "echo \"No tests configured yet\"",
    "lint": "eslint ."
  },
  "keywords": [
    "chrome-extension",
    "image-converter",
    "background-removal",
    "cloudinary",
    "image-processing"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/imagecraft.git"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
